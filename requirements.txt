# ── Document parsing ─────────────────────────────────────
# Using specific extras only — avoids pulling in detectron2/torch
# which are too large for Streamlit Cloud's 1GB RAM limit
unstructured
unstructured[pdf]
unstructured[docx]
unstructured[pptx]
unstructured[xlsx]
pdf2image
pdfminer.six
python-docx
python-pptx
openpyxl
python-magic

# ── LangChain core + providers ───────────────────────────
langchain-core
langchain-google-genai
langchain-groq
langchain-huggingface
langchain-chroma

# ── Vector store ─────────────────────────────────────────
chromadb

# ── ML / embeddings ──────────────────────────────────────
sentence-transformers

# ── App ──────────────────────────────────────────────────
streamlit
python-dotenv
